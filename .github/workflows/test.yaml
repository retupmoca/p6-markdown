name: "Test"
on: [ push, pull_request ]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test with coverage
        with:
          coverage: true
        uses: JJ/raku-test-action@main


